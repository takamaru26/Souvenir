<%= form_with model: [item, item_comment], url: public_item_item_comments_path(item.id), local: false do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに", class: "w-100", id: "comment_textarea" %>
  <%= f.submit "送信" %>
<% end %>