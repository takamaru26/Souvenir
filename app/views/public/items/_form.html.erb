<%= form_with model: @item, url: public_items_path do |f| %>
  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>
  <h4>商品名</h4>
  <%= f.text_field :name %>
  <h4>価格</h4>
  <%= f.text_field :price %>
  
  
  <%= f.submit '投稿' %>
<% end %>